# Zcash Network Configuration
ZCASH_NETWORK=testnet                          # mainnet or testnet
ZCASH_RPC_URL=http://localhost:18232          # Zcash node RPC endpoint
ZCASH_RPC_USER=                                # Optional: RPC username
ZCASH_RPC_PASSWORD=                            # Optional: RPC password
ZCASH_EXPLORER_API=                            # Optional: Custom block explorer API

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost/zcash_htlc
DATABASE_MAX_CONNECTIONS=10

# Application Environment
APP_ENV=dev                                    # dev or prod (auto-runs migrations in dev)